fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
anthropic>=0.69.0
pandas>=3.0.3
openpyxl>=3.1.2
pdfplumber>=0.11.0
reportlab>=4.2.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
python-dotenv>=1.0.1
pydantic>=2.7.1
aiofiles>=23.2.1
httpx>=0.27.0
slowapi>=0.1.9
matplotlib>=3.9.0
structlog>=24.1.0
sentry-sdk>=2.0.0
# Langfuse pinned to the v2 line: this build uses the stable trace()/generation()/flush()
# API. The v3 SDK (OTEL-based) is a deliberate future migration. Optional at runtime —
# the tracing export no-ops unless LANGFUSE_* keys are set.
langfuse>=2.0.0,<3.0.0

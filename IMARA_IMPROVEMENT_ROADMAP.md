# Imara — Improvement Roadmap

**Date:** 21 June 2026
**Basis:** the full project memory (state, product direction, cleanup discipline, economics-agent + verifier-swarm research) + everything shipped this session.

---

## The honest headline

Imara is now **engineering-mature**: feature-complete for its purpose, reliability-hardened, security-hardened, and clean (CI ratchet in place). Across this whole session the recurring finding has been the same — **the bottleneck is no longer features; it's EVIDENCE and DISTRIBUTION.** The Imara Score is a well-built heuristic that has never been calibrated against real funding/repayment outcomes. Until it is, more agents and panels add surface area, not value.

So this roadmap is ordered by *what unblocks the business*, not by what's fun to build. Tier 0 is the real work and it's mostly yours to drive; Tiers 1–3 are what I can build, smallest-risk-highest-value first.

> **Honest tension (added v1.16, self-review):** the flagship below ("Tax Me If You Can") is a *new product*, which sits in direct tension with this headline's core finding that the bottleneck is evidence + distribution, not more surface area. Both can't literally be "the #1 thing." This is flagged, not silently resolved — see **Plan self-review** before the revision log. Your call stands; the plan just shouldn't pretend the contradiction isn't there.

> **Flag (added v1.6):** the **"Imara" name directly conflicts with an established SA/pan-African financial-services group (imara.com)** — a real trademark risk that may force a rebrand. See *Legal & compliance* below. Cheap to check now, expensive to ignore.

> **Cross-cutting rule (non-negotiable): cleanup is part of "done."** Adding features is the addictive part; every feature leaves a little mess — unused files, duplicate logic, code that works but nobody understands — and it compounds. So **every item in every tier below is only complete after an audit-and-prune pass**: find the dead code / duplication / unused components / unnecessary complexity that feature just added, verify each candidate with tools (never delete on the model's say-so), and remove it. This is enforced by the CI ratchet (ruff hard-gate + vulture advisory) and is the real work, not an afterthought. (Researched + grounded: GitClear shows AI-assisted coding measurably raises duplication; software entropy is Lehman's law. Stored as `post-feature-cleanup-discipline`.)

---

## ★ FLAGSHIP TOP PRIORITY — "Tax Me If You Can" (Ruan-directed, 2026-06-21)

A new flagship initiative now sits **above the Imara tiers in priority** (Ruan's call). Full research brief: **`TAX_ME_IF_YOU_CAN_RESEARCH.md`** (extensive cycle — market, competition, legal, migration regimes, AI architecture, business model, risks, phased build).

- **Verdict:** real, large ($34.6B->$97.1B tax advisory; ~142k millionaires relocating/yr), genuinely **underserved** — *mass-affluent mobile earners* sit between Henley-tier human advisory and DIY filing software with no AI-native cross-border tax-optimisation + relocation planner.
- **Unfair advantage:** tax is the textbook case for **Imara's deterministic-first + human-in-the-loop DNA** — the AI-reliability research is unanimous that tax needs a *deterministic rule engine + LLM narration + licensed-professional sign-off* (pure-LLM compounds errors). What transfers is the **architecture and discipline**, not the code. *(Corrected v1.16 — earlier wording "reuse Imara's engine, not a from-scratch build" overstated it.)* Imara's engine computes ~10 SA financial ratios from a known figure set; a cross-border residency/tax-optimisation engine means **codifying the tax law of many jurisdictions plus their interactions** (treaties, CFC, exit tax, CRS, residency tests) — orders of magnitude larger, and it needs **regulatory-change management + dated rule-versioning** as first-class machinery (the multi-jurisdiction-tax-engine research is explicit on this, and that "no tool replaces informed professional judgment"). So: the *pattern* is proven and gives a real head start; the *rule content* is a large, ongoing, net-new build — budget for it honestly.
- **Non-negotiable reframe (the moat, not the obstacle):** lead with **legal tax efficiency + relocation**, NOT "loophole/evasion." The law is what competitors get wrong: GAAR + promoter penalties (SA R100k/mo x2/x3), EU/UK **DAC6/MDR intermediary disclosure** (designing/marketing cross-border arrangements = a reporting "promoter", penalties to GBP1M), licensing (only CPAs/EAs/attorneys advise -> use a **licensed-advisor marketplace**), PI insurance, CRS 2.0/CARF transparency, CFC/exit taxes. Build these in and they become the defensibility.
- **Immediate next step (P0, Ruan/lawyer-led):** pick the launch corridor (e.g. SA/UK/EU earners -> UAE / Greece / Cyprus / Portugal-IFICI), engage a tax lawyer on licensing + DAC6/MDR, line up licensed partner advisers + PI insurance. Then P1 = the deterministic residency/tax engine (apply Imara's pattern + the `sa_knowledge` dated-corpus approach to net-new jurisdiction rules). Full phasing in the brief.
- **P0 also = demand validation FIRST (added v1.16).** The plan's own central lesson is *don't build before you've validated demand* — and this brief currently jumps from "engage a lawyer" to "build the engine" with no customer-discovery step. Before any engine code: confirm the white-space assumption (mass-affluent mobile earners *will pay* for this) with real target-customer conversations / a landing-page or concierge test. Same go/kill discipline as the Imara pilot. Skipping this would repeat exactly the mistake the rest of this roadmap warns against.

*(I'm not a tax lawyer; this is factual landscape + a build plan, not advice. I'll build the legal-planning product, not evasion/loophole schemes.)*

---

## Tier 0 — The actual highest-value work (Ruan-led; I execute the analysis)

**This is the plan. Everything below is secondary.**

> **▶ Plan of action (researched, v1.23):** the binding constraint is **TIME, not features** — outcome-based validation can't be shortcut (backtesting needs outcomes that only accrue over a real outcome period). So the single highest-value move is to **start the outcome clock NOW** via **founder-led design-partner pilots** (the proven first-customer motion — the founder is the best early salesperson; treat first customers as design partners = commitment + learning, not perfect pricing). Bias toward a small **NBFI/lender** partner (generates labelled approve/repay outcomes fastest) **plus 1–2 accountants** (volume + face-validity feedback; the complementary partner who already has SME trust — the most under-used early channel). In parallel — cheap, immediate, no waiting — get **2–3 credit/lending pros to face-validity-review** a batch of Imara reports and lean on the **Altman Z'' convergence already shipped** as interim evidence (best practice = quantitative + expert judgment). Everything buildable (Tax flagship, 1.3 T2, scaling) stays deferred behind this.

1. **Run the design-partner pilot.** The harness is already built and waiting: the `outcomes` table, `services/validation.py` (AUC / Gini / KS / reliability), `services/score_calibration.py` (Platt cold-start, honest "not calibrated until N≥50"), and `IMARA_PILOT_PROTOCOL.md` (the 3-month program with go/kill gates). It just needs **≥30–50 real labelled outcomes**, which means getting Imara in front of an accountant/advisor or a lender. Only you can open that door.
2. **Once outcomes exist, I run Layer-3:** calibrate Score weights against real outcomes, run disparate-impact / fairness testing, and re-issue the model card with *real* discrimination metrics instead of the AHP expert prior. This is what converts Imara from "engineering-mature" to "evidence-backed" — the actual moat, and the thing a lender or regulator will ask for.
3. **Make the GTM call** (it decides what gets built next): **SME-direct** via accountants/advisors, vs **white-label underwriting-intelligence for lenders** (Lula already does 3-month bank-transaction underwriting + embedded distribution — Imara should be the *decision-support layer*, not a lender).
4. **Keep Railway paid** before the trial lapses, or the backend + persistent volume go offline. (Money action — yours.)

---

## Tier 1 — Buildable now, no pivot decision needed (I can do; ranked)

**★ ✅ BUILT + SHIPPED (commit `c5e2f8b0`, live) — Authentication / login (Ruan-directed top priority, 2026-06-21).** *Operator login is built, pressure-tested (0 bypasses), and deployed — currently dormant (`auth_required:false`) until `OPERATOR_PASSWORD` is set on Railway, which ACTIVATES it and closes item 2. Original plan note below.*

**Authentication / login (design).** Ruan wants a login page as the app's #1 priority — and it *is* the fix for the access-control gap (item 2): a login that gates `/api/report/{id}` and the app so reports aren't readable by a bare URL. **One scope decision drives the whole build:** *(a) Operator login* — a single password-gated login for the operator, gating the report + admin surfaces, reusing the dormant `auth.py` Principal seam; proportionate (~1 day), fixes OWASP-A01 now, and does **not** contradict product-direction. *(b) Multi-user / client accounts* — signup + per-user data scoping (the `owner` column already exists) + password reset; this is the **pivot** (login/signup/RBAC are "defer until the GTM call" per product-direction) and is bigger — better to use an auth service (Clerk / Auth0 / Supabase) than hand-roll it. **Recommendation: build (a) operator login now** — it closes the verified #1 security gap and is the proportionate move; treat (b) as the pivot. Once built, item 2 (access-control) folds into this. *(Research: basic auth gate is right for a single-operator app; full signup/RBAC/MFA is overkill until multi-user.)*

1. **✅ BUILT (v1.17) — Prose / narrative verifier** *(from this week's verifier-swarm research — the one genuine win).* Faithfulness guards the *numbers*; the gap is that an LLM *narrative* finding could still contradict the deterministic facts or another agent. Build a deterministic narrative-consistency check (flag findings whose prose contradicts the computed ratios or a corroborated cross-agent finding), optionally with one bounded correction re-prompt. Closes the last anti-hallucination gap in Imara's own deterministic style. **Small, high-confidence, low-risk.**
2. **✅ Access-control the report links — LARGELY CLOSED by the shipped login (v1.12); activates when `OPERATOR_PASSWORD` is set.** *(Reconciled v1.16 — this item was still written as fully open while the login that fixes it has already shipped. The gate middleware now puts `/api/report/{id}` behind the operator principal; setting `OPERATOR_PASSWORD` on Railway is the one remaining step. Residual nuance worth keeping: the **canonical link is still unrevocable/non-expiring** vs share tokens — so for client-facing sharing, prefer issuing revocable share tokens over the raw canonical URL. Below is the original analysis, retained for the rationale.)* Confirmed: analysis IDs are unguessable **UUID4** and *all* `/api/admin/*` endpoints are key-gated, but **`GET /api/report/{id}` had no authorization** — possession of the URL = full read access (a *bearer URL*), and unlike share tokens the canonical link **cannot be revoked or expired**. Per OWASP, an unguessable UUID is *defense-in-depth, not authorization* — links leak via logs, browser history, referrers, shared chats. **Bounded today** (no enumeration; admin gated; no user sessions; operator controls who gets links) but it is a real **Broken-Access-Control** exposure over client financials (POPIA). **Fix:** gate `/api/report/{id}` behind the principal (operator key) *or* a valid revocable/expiring share token, so raw reports aren't readable by bare URL — reusing the dormant `auth.py` Principal seam + the existing share-token model, *without* full login/RBAC. At the pivot, scope queries to the `owner` column (seam already shipped). **~~Highest-priority Tier-1 security item~~ — now resolved by the login pending `OPERATOR_PASSWORD` activation.**
3. **Structured-outputs efficiency (research-refined v1.20).** Drop the second (parse) Claude call by using Anthropic's **native structured outputs** (`output_config={"format":{"type":"json_schema","schema":...}}`) — GA, **no beta header**, and explicitly supported on Imara's models (Sonnet 4.6 + Haiku 4.5); the response is guaranteed schema-valid JSON in `content[0].text`. **Scope correction:** the parse step is **centralized in `base_agent._parse_findings`** (one inherited method), so this is a *contained* change, NOT "touches every agent" as previously written. Two tiers: **(T1, safe)** make the existing parse call structured → guaranteed-valid JSON, removes the fragile fence-stripping + generic-fallback failure mode (same call count, pure reliability win); **(T2, the ~40% cost win)** eliminate the 2nd call by having `analyze()` return structured findings in one call — optionally pairing **extended thinking** so the model still reasons freely before the grammar-constrained output (the docs confirm grammar state resets between thinking and final output). **Gate:** T2 changes finding *quality*, which can only be judged with live runs — so it's gated on the local A/B in `live_verify.py` (the sandbox can't make keyed calls by design). Needs a recent `anthropic` SDK (sandbox-verified: 0.111.0 supports `output_config`). **Do it as its own careful cycle with the cleanup ratchet.**
4. **✅ BUILT (v1.17) — Observability — structured logging + error tracking (production-stack Layer 12).** Two complementary halves, both real gaps:
   - **(a) Structured logging.** Imara still uses ad-hoc `print()` with no levels and no correlation — "you can't fix what you can't see." Replace with structured JSON logging (`structlog`, routed through stdlib so uvicorn logs match), tagging every line with the existing **`analysis_id` as the correlation/request ID** + proper levels (debug / info / error). `tracing.py` already uses contextvars, so structlog's contextvars binding drops in cleanly, and **Railway's built-in log viewer already centralises stdout** — so this is mostly *structure + levels*, not new infrastructure.
   - **(b) Error tracking.** Wire Sentry (free tier is plenty at this scale) for exception capture + alerting at the FastAPI layer + a frontend hook; the structured logs become the breadcrumbs around each captured error (Sentry now ingests both). 
   - *Honest scope:* a heavy aggregation stack (ELK / Loki / Datadog / Splunk) is **premature** — Railway's viewer + Sentry suffice until query patterns demand more (that lands in Tier 2). **Small, high-value, and useful immediately for debugging — not just at scale.**
5. **✅ BUILT (v1.17) — Database backup + restore (production-stack Layer 13).** The Railway volume makes the SQLite DB survive redeploys, but it is **not backed up** — losing the volume loses every client analysis, share link, and the tamper-evident audit chain. Add a scheduled dump to off-volume storage + a documented restore. Essential for a tool holding client financials. **Small–medium, high-value.**
6. **✅ BUILT (v1.17) — Harden the `Ask Imara` open LLM endpoint (the McDonald's surface).** `Ask Imara` (`POST /report/{id}/ask`) is Imara's one free-text LLM endpoint — where a user could go off-topic ("write me Python") or rack up tokens, exactly the McDonald's-chatbot failure. It already has caps (500-char question, 600 max-tokens, cheap Haiku, grounded + injection-resistant prompt), but lacks: a **scope/topic guard** that rejects non-report questions *before* the LLM call (blocks off-topic abuse and saves cost — the research's "pre-generation" guardrail), a **dedicated rate-limit** on `/ask` (per report/IP, beyond the global limit), and it doesn't yet run the shipped **`input_guard`** over the question text (injection/PII). Optional output-side PII check is lower priority (it analyses the client's own data). **Small, reuses existing pieces.** *A commercial LLM firewall / AI gateway (Cloudflare AI Gateway, Lakera) is overkill at operator scale — DIY guardrails suffice for a small single-scope app; revisit at the multi-user / public-API pivot (Tier 2).*

---

## Tier 2 — Durability / scale (a concrete playbook; build at the volume inflection, not before)

Scaling is what a software engineer adds once a vibe-built product gets traction, and the failure modes are predictable. Honest stance for Imara: it is at **operator scale (~1–10 effective users)** today, so most of this is **premature now** — but the *plan* is cheap to write and expensive to retrofit under fire, so it lives here with Imara's real ceilings named.

**Imara's actual scaling ceilings (not the generic ones):**
- **SQLite write-lock is the first ceiling — not threads.** SQLite uses file-level locking → "database is locked" when analyses write concurrently (Postgres/MVCC fixes it). `database.py` already serialises writes with a lock; fine at operator scale, bites around 10–100 concurrent users.
- **Single backend instance** (one Railway container) → no horizontal scale / load balancing yet.
- **In-process background tasks** (no durable queue) → a restart kills in-flight runs (flagged, not resumed).
- **No DB backups** → the one do-now item (Tier 1.5): losing the volume loses all client data + the audit chain.
- **NOT a ceiling — don't "fix" it:** the "single-thread Python" trap does **not** apply here. The agents are **I/O-bound** (waiting on the Claude API) and Python **releases the GIL during network I/O**, so the existing ThreadPool wave fan-out is the *correct* design for this workload. Rewriting to multiprocessing would add complexity for no gain.

**Per-tier playbook (build a row when you cross into it):**

| Users | Dominant bottleneck | What to do |
|---|---|---|
| ~1–10 (now) | none structural | do now: DB backups (1.5) + error tracking (1.4); SQLite **WAL mode** is a cheap concurrency stopgap |
| ~10–100 | SQLite write-lock; restart loses jobs | durable **job queue** (arq/RQ/Celery) + concurrency cap; **migrate to Postgres**; keep the `/api/health` check |
| ~100–1k | single instance | multiple workers behind a **load balancer**; Postgres connection pooling; response/result caching |
| ~1k–10k+ | DB + compute | autoscaling workers; Postgres **read replicas**; CDN (already on Vercel); queue scaling; per-tenant isolation (seams already shipped) |

- **EconomicsAgent P3 (live data):** wire the free World Bank / SARB refresh (cache + visible as_of) when current macro data becomes operationally important. (P4 calibration folds into Tier 0.)

---

## Tier 3 — Pivot-gated (do NOT build until the GTM call is made)

Per the product-direction memory, the cheap future-proofing seams are **already shipped** (owner/tenant column, auth Principal, `/api/v1`, stable Score contract, usage metering, MULTI_TENANT/PUBLIC_API flags — all dormant). So the eventual pivot is a feature-add, not a rewrite. Deferred until decided: real login/signup, billing, full RBAC, OAuth, B2B API productization, B2C self-serve. **Building any of these now would be premature.**

*Tooling note (Google Stitch / AI UI generation).* The existing Imara frontend is mature, WCAG-reviewed and domain-specific — the UI research already concluded it needs **no overhaul**, so a generative-UI tool should **not** be pointed at it (it would discard the accessibility + trust/explainability surfaces, clash with Imara's hardcoded-Tailwind conventions, and add cleanup churn; the research notes these tools need a formal design-token system for fidelity, which Imara doesn't have). Where a free generator like **Google Stitch** (MCP → Claude Code; real + free as of 2026) *could* earn its place is **net-new greenfield consumer surfaces at the pivot** — a B2C onboarding flow or a marketing site — as a first-draft accelerator, with the caveats that generated code needs WCAG validation, integration into Imara's component conventions, and a cleanup pass. Judge it as a build-tool, not an Imara feature.

---

## Production-stack coverage (13-layer check)

A popular framing says a real product needs ~13 layers, not the 2 (front end + DB) of a demo. Audited honestly, **Imara already covers ~10 of 13 solidly — it is a real product, not a demo.** The 13-layer list is a useful checklist (it aligns with the 12-factor app + Google SRE production-readiness reviews), but "all 13 or it's a demo" overstates it: depth per layer should match *stage*, and for a small product the evidence says do **observability + auth + backups before** load balancing / heavy caching / auto-scaling (which are premature now).

| # | Layer | Imara status |
|---|---|---|
| 1 | Front-end foundations | OK — React/Vite on Vercel |
| 2 | APIs & back-end logic | OK — FastAPI (46 routes) |
| 3 | Database & storage | OK — SQLite on Railway persistent volume (Postgres deferred to scale) |
| 4 | Auth & permissions | PARTIAL — admin gate + expiring/revocable share tokens + dormant Principal seam; canonical report link still open -> **Tier 1.2** |
| 5 | Hosting & deployment | OK — Railway + Vercel, auto-deploy from `main` |
| 6 | Cloud & compute | OK — Railway containers |
| 7 | CI/CD & version control | OK — GitHub + Actions CI + cleanup ratchet |
| 8 | Security & row-level security | OK — input guard, hash-chained audit log, POPIA framing, rate-limited; RLS = `owner` column (dormant by design in operator mode) |
| 9 | Rate limiting | OK — slowapi, X-Forwarded-For keyed; `/ask` capped |
| 10 | Caching & CDN | PARTIAL — Vercel CDN (frontend); minimal backend caching — fine at this scale, not a gap |
| 11 | Load balancing & scaling | DEFERRED — single instance + internal parallel waves; **Tier 2 (premature now)** |
| 12 | Error tracking & logs | GAP — cost/usage tracing + fleet-quality, but no exception tracking/alerting -> **Tier 1.4** |
| 13 | Availability & recovery | GAP — volume persistence + interrupted-run flagging, but no DB backups / no resume -> **Tier 1.5 (backups) + Tier 2 (resume)** |

## Legal & compliance (Ruan-led; needs a lawyer — I can draft, not advise)

The circulating advice is consumer-app / Apple-Store / FTC / GDPR-CCPA framed. Imara is a **SA B2B web tool processing client financial data**, so the honest localisation: **POPIA is the primary regime**; US/EU laws apply only by where the *data subjects* are (pivot-gated).

1. **⚠ Trademark — the standout finding (check now).** "Imara" is **already an established pan-African financial-services group** (Imara Group / Holdings / Capital / Asset Management — imara.com) operating in SA, Botswana, Zimbabwe and the UK in fintech/asset management since the 1950s. That's a *direct* conflict — same name, same industry, same region — the classic cease-and-desist trigger. **Action: proper trademark clearance (CIPC trademark search + legal opinion) before any go-to-market; be prepared a rebrand may be needed.** Cheap to check now, very expensive to ignore after building brand equity. **Possibly the most consequential single item in this roadmap.**
2. **✅ DRAFTED v1.27 — POPIA posture (primary; partly needed now).** Imara *advises clients* on POPIA (SALegalAgent) but must hold its own: a published **privacy policy**, a designated **Information Officer** (mandatory), a **lawful basis** + client consent, a **retention policy**, a **breach-notification plan** (72h), and **data-processing agreements** with clients. Technical hooks already exist (PII redaction in the input guard, the hash-chained audit log, decision-support framing, DB retention) — the documents + roles do not. Fines up to **R10M**. Grows more pressing as client-facing exposure increases.
3. **✅ DRAFTED v1.27 — Terms of Use / liability shield.** Needed before any client-facing or self-serve use. The *analysis output* already carries the decision-support / not-an-NCA-credit-decision / not-FAIS-advice disclaimer (`governance.py`) — that shields the verdicts — but a site/service **Terms of Use** (acceptable use, liability limitation, no-warranty) is a separate, missing document.
4. **GDPR / CCPA — scoped, not now.** Apply by where the *data subjects* are, so only bite if/when Imara serves EU or California users (pivot-gated; note CCPA's B2B exemption expired 2023). N/A at current SA-only operator scale.

**What I can vs can't do:** I can *draft* a POPIA-aligned privacy policy + Terms of Use and wire technical hooks (consent capture, retention enforcement, an Information-Officer contact). I cannot give legal advice — all of this needs review by an SA attorney (ties to the SA fintech-law sign-off already noted as Ruan-led).

---

## Adjacent / new-venture ideas (evaluated — kept separate from Imara core)

*Ruan floats expansion ideas here; I research each and note honestly whether it's an Imara feature, a separate venture, or not worth it — keeping the core plan focused.*

### "Tax me if you can" — AI tax-optimisation + tax-migration (2026-06-21)
**Verdict (updated 2026-06-21): ELEVATED to FLAGSHIP top priority** (see the flagship section at the top of this plan + `TAX_ME_IF_YOU_CAN_RESEARCH.md`). Still a *distinct product* from Imara core, built on Imara's engine. The detail below stands — the framing needs a hard legal line, and the pitch ("find every loophole," "mass-migrate people for tax") must be reframed to legal tax efficiency + relocation:
- **Legal line.** Tax *avoidance* (legal planning) vs *evasion* (illegal) is everything; the product must stay firmly on legal planning. SA's **GAAR** (Income Tax Act ss 80A-80L) lets SARS disregard any arrangement whose *sole/main* purpose is a tax benefit with no commercial substance — i.e. exactly "pure loophole" structures. SARS is aggressive (won a R46M GAAR case Oct 2025).
- **Existential business risk: promoter penalties.** In SA, designing/marketing a tax-avoidance arrangement makes you a **"promoter"** -> **R100,000/month** (up to 12 months), **doubled** if the benefit >R5M, **trebled** >R10M, plus reportable-arrangement disclosure duties. An AI marketing avoidance schemes at scale is squarely here.
- **The migration plank is partly outdated.** Malta **scrapped citizenship-by-investment in July 2025** (after the ECJ ruled against it) -> now "citizenship by merit." Residency-by-investment still exists (Malta golden visa, UAE 0%-tax + golden visa, Swiss lump-sum CHF 200-600k/yr), but **residence != tax residency**, and **CRS transparency + exit/CFC taxes** make "mass tax migration" far more complex than the pitch. Needs tax-law + immigration-law expertise per jurisdiction.
- **Name:** "Tax me if you can" — run a trademark/clearance check (same lesson as the Imara name).

**The one legitimate Imara adjacency — ✅ BUILT v1.25:** a defensible **SME tax-OPTIMISATION layer** — the flip side of Imara's existing SA-Tax *compliance* agent. Legal planning only (is the SME claiming the SBC / Section 12E rates, ETI, allowances it *qualifies* for? legitimate structuring it's missing?), framed as compliance-positive + GAAR-respecting + professional sign-off — NOT loophole-hunting. Plausible future Imara feature; the broader venture stays separate and lawyer-led. *(Not tax advice; I won't help design/market avoidance schemes — the promoter-penalty zone — but a legal SME tax-optimisation feature is fair game.)*

---

## Standing disciplines (already in force — keep them)

- **Post-feature cleanup ratchet:** after every feature, audit + prune (CI now runs ruff as a hard gate + vulture advisory). Verify dead-code findings with tools before deleting.
- **Deterministic-first / anti-hallucination DNA:** numbers by arithmetic, the LLM only narrates; verify against the source (this is why Imara already beats the "LLM-checks-LLM swarm" pattern).
- **Research-before-build; don't add agents for their own sake** (the verifier-swarm research showed heterogeneous specialists + a deterministic verifier already sit in the evidence-backed sweet spot — a bigger swarm would just add correlated-error cost).
- **Coding rulebook (the 4 Karpathy agent rules) — adopt explicitly:** (1) *Think before coding* — state assumptions, surface ambiguity, no silent guesses; (2) *Simplicity first* — minimum code that works, no speculative abstractions/future-proofing; (3) *Surgical changes* — touch only what the task requires, no drive-by refactors/renames/unrequested comments; (4) *Goal-driven execution* — define "done" + a verification before starting, verify before stopping. These formalise what's already practised here and complement the cleanup ratchet. **Cheap near-term action: codify them into the repo's `CLAUDE.md` / `AGENTS.md`** so every agent (future sessions, other tools) is bound by them. (Source rules are credible best practice; the "41%->3%" figure is one practitioner's informal measurement — direction sound, exact % not gospel; use as a menu, not a blind template.)
- **Dev-workflow tooling — adopt skills + Stop hooks; decline multi-agent parallel.** Worth adopting (they make the disciplines automatic, not memory-dependent): a **Stop hook** that runs `ruff` + `vulture` + the test suite and *refuses to stop on failure* — auto-enforcing "verify before stopping" (rule 4) + the cleanup ratchet *locally*, mirroring CI; and **skills** to encapsulate Imara's recurring workflows (the pressure-test pattern, the mount-safe string-replace+`ast.parse` edit, the push-staging routine, research-cycle→roadmap). *Caveat:* both are authored in Claude Code / Settings → Capabilities, not in a Cowork session. **Declined for Imara:** running multiple Claude Codes in parallel + offloading prompt-writing to ChatGPT — research caps useful parallelism at 2–4 agents and flags merge-conflict + coordination overhead as "especially significant" for solo/single-repo work (Imara's hotspots: `main.py`, `specialist_agents.py`); the "ChatGPT writes better prompts" claim is anecdotal, and the useful kernel (markdown context) is already `CLAUDE.md`. If ever parallelising, use **git worktrees** with non-overlapping files, not multiple agents on one tree.
- **AI code review on PRs — adopt (high-value here specifically).** Add an AI reviewer to the PR pipeline: Claude Code's native **Code Review / `/security-review` GitHub Action** (lowest friction — already on Claude + GitHub Actions), or **CodeRabbit**, or **Sourcery** (strong Python for the backend). *Why it matters more for Imara than most:* much of the codebase is AI-generated, and research finds AI-written code carries **~75% more logic errors + 3x more readability issues** than human code — exactly what ruff/vulture can't catch (they do style/dead-code; an AI reviewer does **logic, architecture, security**). Imara is now security/financial-sensitive (auth shipped, POPIA data, hard-deletes, the audit chain). **Customise the prompt for architecture-not-syntax:** business-logic correctness, injection vectors, unhandled edge cases, N+1s; ignore style (ruff owns it); explicitly **flag any change touching auth, the audit log, data deletions, or the financial/Score engines.** **Severity gating** mirrors the existing pattern (critical blocks merge, warnings advisory — like ruff-hard / vulture-advisory). *Adoption cost (honest):* needs a shift from solo direct-to-main (`push_imara.bat`) to a feature-branch -> PR -> review -> merge flow for non-trivial changes; trivial changes can stay direct. *The "second net" (canary): Imara has no staging env (Railway auto-deploys main->prod) — a staging/canary step is a separate **Tier 2** infra item, arguably premature for a solo operator now.*

---

## If you want me to start building now

My recommendation, in order: **(1) set `OPERATOR_PASSWORD` on Railway** (zero-code — activates the shipped login and closes the access-control/POPIA gap; this replaces the old "report-link access control" build, now done), **(2) the prose verifier** (closes the last hallucination gap), **(3) error tracking + DB backups** (cheap operational wins surfaced by the 13-layer audit), then **(4) structured-outputs** (cost/latency) as its own cycle. But none of these move the needle as much as **Tier 0** — the pilot — which is the one thing only you can unblock.

---

## Plan self-review (v1.16) — does this plan hold up?

*You asked me to check the plan for correctness instead of just extending it. I interrogated it and ran a research cycle on the two assumptions the critique hinged on. Verdict: structurally sound and requirements-complete, but it had one real contradiction, one overstatement, one missing step, and some staleness. All now fixed above. The honest findings:*

1. **Contradiction — "stop adding features" vs a new flagship product (the big one).** For 15 revisions the through-line has been: Imara is engineering-mature, the bottleneck is *evidence + distribution*, don't add surface area. Then v1.14 put an entirely new product at the very top. That's the maximal surface-area move, not a panel. Research backs the worry — before product/market fit the consensus is *focus obsessively on one thing*; "ruthless prioritisation of 1–2 goals" and the "shiny-object" trap are exactly this pattern. **Not mine to overrule — it's your strategic call — but the plan now states the tension openly** (headline flag) instead of pretending Tier 0 and the flagship are both "#1." The cleanest reconciliation if you want one: treat the flagship as a *deliberate pivot bet* that supersedes the validate-Imara thesis, OR sequence them (validate Imara's pilot to a go/kill gate first, then commit to the flagship) — rather than running both half-resourced as a solo operator with the Railway trial lapsing.

2. **Overstatement — "reuse Imara's engine, not a from-scratch build."** Tempered above. Imara computes ~10 SA ratios; a cross-border tax engine means codifying many jurisdictions' law + interactions + dated rule-versioning + regulatory-change management. The *architecture/discipline* transfers; the *rule content* is a large net-new, ongoing build. The plan now says so.

3. **Missing step — the flagship skipped demand validation.** The plan's own central lesson. P0 went lawyer → build engine with no customer-discovery gate. Added one.

4. **Staleness — Tier 1.2 (report-link access control) was still written as fully open** while the login that fixes it shipped in v1.12. Reconciled (✅ closed pending `OPERATOR_PASSWORD`); the "start building now" list updated so its #1 is now the zero-code activation, not a rebuild.

**What held up well:** every one of your 15 inputs was researched-then-folded with a dated revision entry (requirement met); the tier structure and deterministic-first DNA are intact; the genuinely valuable gaps it surfaced (trademark conflict, error tracking, DB backups, Ask-Imara hardening, OWASP A01) are real and correctly prioritised; and it has consistently declined hype (ruflo, verifier-swarm, multi-agent-parallel, UI regeneration) on evidence. The plan's bias is sound — its one blind spot was going soft on the flagship's size and focus-cost because it was a direct directive, which this review corrects.

---

## Revision log

*This is a living plan. Each new input from Ruan triggers a research cycle first, then a grounded update here.*

- **2026-06-21 — v1 (baseline).** Initial roadmap from full project memory + this session (per-agent P0/P1/P2, input guard, cleanup + CI ratchet, verifier-swarm research). Tier 0 = pilot → Score calibration + GTM; Tier 1 = prose verifier, report-link access control, structured-outputs; Tier 2 = durability/scale; Tier 3 = pivot-gated (seams already shipped). Workflow: new info → research cycle → update this plan.
- **2026-06-21 — v1.1.** Ruan re-emphasised the cleanup philosophy. Elevated "cleanup is part of done" to a non-negotiable cross-cutting rule applied to every tier (not just a standing-discipline footnote). Passage already research-grounded earlier (GitClear / Lehman) and stored as post-feature-cleanup-discipline — reinforced, not re-researched.
- **2026-06-21 — v1.2.** New input: the "13-layer production stack" framework. Research cycle (12-factor / Google SRE PRR + small-SaaS prioritisation) → it's a sound checklist, but "all 13 or it's a demo" overstates it. Audited Imara against all 13: **~10/13 solid** (not a demo). Genuine near-term gaps surfaced = **error tracking/alerting (Layer 12)** and **DB backup/restore (Layer 13)** → added as Tier 1.4 and 1.5. Confirmed load balancing / heavy caching / auto-scaling (Layers 10–11) stay Tier 2 (premature). Added the coverage table.
- **2026-06-21 — v1.3.** New input: the viral 4-rule Karpathy CLAUDE.md (ForrestChang / andrej-karpathy-skills, ~100k+ stars). Research cycle: file + rules real; the 41%->3% claim is the *extended 12-rule* version and is an informal practitioner measurement (direction sound, not a controlled study; "menu not template"). These are coding-discipline rules (how the agent works), already largely practised here. Added them as an explicit standing-discipline coding rulebook + a cheap action to codify into the repo CLAUDE.md/AGENTS.md. No product-feature change.
- **2026-06-21 — v1.4.** New input: scaling across user tiers (vibe-coder failure modes — single container, no backups, single-thread Python). Research cycle confirmed: SQLite file-lock is a real concurrency ceiling (Postgres/MVCC + job queue is the fix); BUT the "single-thread Python" trap does NOT apply to Imara — agents are I/O-bound and the GIL is released during network I/O, so the ThreadPool fan-out is correct (don't rewrite to processes). Turned Tier 2 into a concrete per-tier scaling playbook with Imara's real ceilings named (SQLite write-lock, single instance, no queue, no backups). Reaffirmed scaling is gated on the volume inflection (premature now); backups remain the one do-now item.
- **2026-06-21 — v1.5.** New input: logging / observability ("can't fix what you can't see"). Research: structured logging (structlog/JSON, correlation IDs, levels) is the FastAPI best practice and is *complementary* to error tracking (logs = breadcrumbs + non-exception "it just stopped"; Sentry = exception capture + alerts). For a small app, structured logs to stdout + Sentry free tier is the right scope; ELK/Loki/Datadog is premature. Refined Tier 1.4 from "error tracking" into a combined observability item (structured logging via structlog + analysis_id as correlation ID + levels, routed to Railway's existing log viewer; Sentry for errors; heavy aggregation deferred to Tier 2). Imara already has the seam (analysis_id flows through; tracing.py uses contextvars).
- **2026-06-21 — v1.6.** New input: app legal/compliance (privacy policy, ToU, data law, IP). Research + SA-localisation: POPIA is Imara's primary regime (GDPR/CCPA only at a pivot serving EU/CA users); fines to R10M; Imara has technical hooks but lacks its own privacy policy / Information Officer / retention policy / ToU. **Material finding: the "Imara" name conflicts with an established pan-African financial-services group (Imara Group, imara.com) — a direct same-name/same-industry trademark risk that may force a rebrand; flagged as do-now clearance check.** Added a Legal & compliance section (Ruan-led / lawyer-required; I can draft policy + ToU, not advise) + a headline flag.
- **2026-06-21 — v1.7.** New input: Google Stitch (AI UI generation) + Claude Code via MCP. Research: tool is real/free (Google Labs, MCP export, design.md tokens), but design-to-code is good at consistency not distinctiveness, needs WCAG validation, and needs a token system for fidelity. Verdict for Imara: NOT for the existing frontend (already mature/WCAG/domain-specific; UI research said no overhaul; regeneration would discard accessibility + trust surfaces and add churn — conflicts with the simplicity/surgical/cleanup disciplines). Candidate only for net-new greenfield consumer surfaces at the pivot (Tier 3), as a build-tool with caveats. Added a Tier-3 tooling note; no product-architecture change.
- **2026-06-21 — v1.8.** New input: a Claude Code dev-workflow tip (ChatGPT-as-prompt-writer + parallel Claude Codes; skills; stop hooks). Research: skills + hooks are real Claude Code features (hooks block deterministically, skills are skippable; a Stop hook can refuse to stop until tests pass). Verdict for Imara — adopt **skills** (encapsulate recurring workflows) + a **Stop hook** (run ruff/vulture/tests, block stop on failure → auto-enforce the disciplines locally, mirroring CI); **decline** multi-Claude-parallel + ChatGPT-prompt-offload (solo/single-repo merge + coordination overhead outweighs gains per research's 2–4-agent ceiling; the prompt claim is anecdotal; markdown context already = CLAUDE.md). Added a dev-workflow bullet to standing disciplines. Dev-workflow input — no product change.
- **2026-06-21 — v1.9.** New input: "LLM firewall" (McDonald's chatbot abused to write Python / burn tokens). Research: LLM firewall = input/retrieval/output guards + topic scope + cost budgets, but **DIY guardrails suffice for a small single-scope app**; commercial firewall is for complex multi-user/tool-using systems; block off-topic *before* the LLM call to save cost. Mapped to Imara: the analysis pipeline is already guarded (input_guard + deterministic core + faithfulness), so the residual surface is the open `Ask Imara` (/ask) endpoint — has caps but no scope guard / dedicated rate-limit / input_guard-over-question. Added Tier 1.6 (harden Ask Imara with cheap guardrails, reuse existing pieces); commercial AI gateway = Tier 2/pivot. "Buy a firewall / stop thinking about it" overstated — Imara already has most of it.
- **2026-06-21 — v1.10.** New input: an access-control security probe (URL reuse / logout / "is this a security issue?"). Verified against code: analysis IDs are UUID4 (unguessable) and all /api/admin/* are key-gated, but GET /api/report/{id} has NO authz (bearer URL) and the canonical link is unrevocable/non-expiring (only share tokens can be revoked); no user login/sessions (operator mode). Research: OWASP A01 — unguessable UUID is defense-in-depth, NOT authorization (links leak). Verdict: real but bounded Broken-Access-Control gap, exactly Tier 1.2 — sharpened with verified facts + OWASP framing + the unrevocable-canonical-link nuance, and bumped to highest-priority Tier-1 security item. No new item; existing gap confirmed + elevated.
- **2026-06-21 — v1.11.** Ruan directive: build a login page, top priority. Research: for a single-operator app a basic login gate is the right scope (full signup/roles/MFA overkill until multi-user; if multi-user, buy not build). Elevated Authentication/login to the TOP of Tier 1 — it is the fix for the access-control gap (1.2): a login that gates /api/report/{id}. Build hinges on one scope decision: (a) operator login now (proportionate, ~1 day, fixes OWASP-A01, on-strategy) vs (b) multi-user accounts (the pivot, bigger, use an auth service). Recommended (a); asked Ruan to confirm scope before building.
- **2026-06-21 — v1.12.** BUILT the operator login (Ruan-directed top priority) and shipped it (commit c5e2f8b0, live; auth_required field now in /api/health). Multi-user-ready: dependency-free HMAC bearer token carrying `sub` → pivot resolves it to a tenant via the Principal seam, no endpoint changes. Gate middleware protects report/analyze/status/simulate; public share links, the demo (precise path-segment match), admin (own key), /v1 and health stay open. Backward-compatible: OFF until OPERATOR_PASSWORD is set. Frontend Login.jsx + token handling + gate (share-token links exempt). Pressure-tested: forged/empty/tampered/expired tokens + demo-substring bypass all → 401, 0 bypasses; cleanup ratchet clean; 190 tests pass (5 new). **ACTION FOR RUAN: set OPERATOR_PASSWORD on Railway to activate.**
- **2026-06-21 — v1.13.** New input: a NEW product idea, "Tax me if you can" (AI that finds tax loopholes + mass tax-migration to Malta/Dubai/Switzerland). Research: real market, but verdict = SEPARATE venture, not an Imara feature. Legal reality: avoidance(legal) vs evasion(illegal); SA GAAR targets pure-loophole structures (SARS won R46M GAAR Oct 2025); **promoter penalties R100k/mo x2/x3** make a loophole-marketing AI an existential legal risk; **Malta scrapped CBI July 2025** (ECJ); residence!=tax residency + CRS/exit taxes complicate migration. Added an "Adjacent / new-venture ideas (evaluated)" section. The one on-brand Imara adjacency: a legal SME tax-OPTIMISATION layer (flip side of the SA-Tax compliance agent, GAAR-respecting, pro sign-off). Kept separate from Imara core.
- **2026-06-21 — v1.14.** Ruan directive: make "Tax Me If You Can" the utmost top priority + do extensive research. Ran a deep multi-angle research cycle (10+ searches: market, competition/white-space, legal/regulatory across SA/EU/UK/US, migration regimes UAE/CH/GR/IT/CY/PT/MT, AI feasibility, business model, risk) -> wrote `TAX_ME_IF_YOU_CAN_RESEARCH.md`. Findings: large underserved market (mass-affluent mobile earners between Henley HNW advisory and DIY filing software); tax is the textbook case for Imara's deterministic+human-in-loop DNA; compliant reframe is the moat (GAAR/promoter penalties, DAC6/MDR intermediary disclosure, licensing -> licensed-advisor marketplace, PI insurance, CRS/CFC/exit-tax). Added a FLAGSHIP TOP PRIORITY section above Tier 0; updated the adjacent-idea entry to point to it. Held the legal line (legal planning, not evasion).
- **2026-06-21 — v1.15.** New input: AI code review in the PR pipeline (CodeRabbit/Sourcery/Claude Action; architecture-not-syntax prompt; gate merges on review; pair with canary). Research: real + effective; AI-written code has ~75% more logic errors / 3x readability issues than human code (high-value given Imara is largely AI-generated) — and it catches the LOGIC/architecture/security layer ruff/vulture can't. Added as a standing-discipline dev-workflow item: adopt an AI PR reviewer, prompt for logic/security + flag auth/audit/deletes/financial engines, severity-gate (critical blocks / warnings advisory). Honest friction: needs a PR flow vs solo direct-to-main; canary/staging = separate Tier-2. Complements the CI ratchet.
- **2026-06-21 — v1.16 (self-review, Ruan-requested).** Ruan asked me to audit the plan for correctness rather than extend it. Ran a research cycle on the two load-bearing assumptions (founder focus / building a second product before validating the first → consensus is *focus obsessively pre-PMF*, shiny-object trap; and multi-jurisdiction tax-engine complexity → regulatory-change mgmt + dated rule-versioning are first-class, "no tool replaces professional judgment"). Fixes: surfaced the **headline contradiction** (stop adding surface area vs a new flagship product) as an explicit tension flag + a Plan self-review section; **tempered** the "reuse Imara's engine" claim; added a **demand-validation P0 step** to the flagship; **reconciled stale Tier 1.2** (login already closes it). Confirmed all 15 prior inputs were researched-then-folded; tiers + deterministic-first DNA intact.
- **2026-06-21 — v1.17 (BUILD: Tier 1 hardening batch, Ruan-directed "implement, free hand").** Shipped the four low-risk buildable Tier-1 items, each tested + ratchet-clean (full suite **219 pass**, +29 new tests; ruff F401/F811/F841 + vulture clean): **(1.1) Prose verifier** (`services/prose_verifier.py`) flags findings whose qualitative narrative contradicts the computed ratio status — direction-independent health lexicon, flags only positive-vs-critical / negative-vs-good, negation guard; wired into the pipeline + a Narrative-check badge in the UI. **(1.5) DB backups** (`services/backup.py`) consistent SQLite online-backup snapshots + rotation + restore, daemon scheduler (BACKUP_ENABLED, default off), admin endpoints, `BACKUP_RESTORE.md` runbook. **(1.6) Ask-Imara hardening** pre-LLM deterministic scope guard (blocks off-topic abuse before an API call) + shared input_guard over the question (the dedicated /ask rate-limit already existed). **(1.4) Observability** (`services/obs.py`) structlog JSON logging routed through stdlib, analysis_id/request_id correlation, request-logging middleware, all 16 print() converted, optional Sentry (SENTRY_DSN). New deps: structlog, sentry-sdk. **Deliberately DEFERRED: structured-outputs (1.3)** — it changes real Anthropic API behaviour across every agent, which can't be verified from the MOCK_MODE sandbox; shipping blind would violate "implement correctly," and the plan already flagged it as its own careful cycle needing live-API verification. All four shipped items are opt-in / backward-compatible. **(Process note: this session also caught + repaired an Edit-tool truncation of this file's revision-log tail; switched to mount-safe writes — reinforces the CLAUDE.md constraint.)**
- **2026-06-21 — v1.18 (PRESSURE TEST cycle, Ruan-directed).** Adversarially probed the four v1.17 items. Found + fixed one **real false-positive in the prose verifier**: proximity scanning flagged "a strong leadership team and a current ratio of 0.4x" (the adjective modifies *leadership*, not the ratio). Replaced proximity with **grammatical adjacency** — the health word only counts if joined to the metric phrase by an uninterrupted run of connector tokens (articles/copulas/numbers), with conjunctions/punctuation/other nouns as hard boundaries; fixed a follow-on bug where a decimal ("0.4x") was mis-read as a clause boundary. Broadened the **Ask-Imara scope guard** to block off-topic verbs beyond "write" (compose/create/generate/draft/make/build) with a bounded, ReDoS-safe pattern (3ms on a 10k pathological input); confirmed no over-blocking of legit finance asks. **Verified clean (no fix needed):** observability context isolation — analysis_id does NOT leak across thread-reused executor tasks (the finally-clear holds), request_ids unique, Sentry init crash-safe both ways; DB backup consistent under concurrent writes, corrupt-snapshot restore rejected before touching the live DB, prune edges. Added 10 regression tests (full suite **229 pass**); ratchet clean. (Process: roadmap edits now mount-safe after the v1.17 Edit-tool truncation.)
- **2026-06-21 — v1.19 (LIVE API verification, Ruan-directed).** Attempted a real Anthropic call from the sandbox: the key is present in `.env` and looks valid, but the call fails with **APIConnectionError** — the Cowork sandbox's egress proxy does not allowlist api.anthropic.com (a non-auth network block). So live-API behaviour genuinely cannot be exercised here, which **confirms the v1.17 reason for deferring structured-outputs (1.3)**. Delivered a ready-to-run harness, **`backend/live_verify.py`**, for Ruan to run locally (where the key + internet work): it verifies connectivity, Ask-Imara (live grounded answer + off-topic blocked pre-call), and — the key de-risk — **structured-outputs feasibility via tool-use** (single call returns schema-valid findings, proving the 2nd parse call can be dropped); `FULL=1` also runs the whole `/api/analyze` pipeline live and asserts the report assembles with both faithfulness + prose-verifier summaries over REAL findings. **Tier 1.3 stays gated until that harness passes locally** — then it's a safe build.
- **2026-06-21 — v1.20 (research cycle: live-verify path + structured outputs, Ruan-directed).** Ruan delegated the choice to research. Findings: (a) the live API genuinely **cannot** run from the Cowork sandbox — diagnosed precisely: unauthenticated requests reach Anthropic (Cloudflare cf-ray + JSON), but any request **carrying the API key** is bounced by the sandbox proxy with a plain `Unauthorized` (a by-design secret-exfiltration guard; not the key, not SSL, not the code). So verification belongs locally or on Railway. (b) The bigger find — Anthropic shipped **native structured outputs (GA)** via `output_config.format`: grammar-constrained, guaranteed-valid JSON, no beta header, supported on Imara's Sonnet 4.6 + Haiku 4.5. This is the correct mechanism for 1.3 (better than the tool-use workaround), and the parse step is **centralized in `base_agent._parse_findings`**, so 1.3 is contained, not sprawling. Actions taken: upgraded `live_verify.py`'s structured-output probe to native `output_config.format` (tool-use fallback for older SDKs); refined the Tier 1.3 plan (T1 safe reliability win vs T2 call-elimination gated on a local quality A/B); confirmed SDK 0.111.0 supports it. **Key is now valid + in `.env`** (Ruan pasted it; verified `sk-ant-`/108 chars without reading the value). Next: run `live_verify.py` locally → that result gates the 1.3 build.
- **2026-06-21 — v1.21 (LIVE verification PASSED, run on Ruan's machine).** Ran `run_live_verify.bat` locally (the sandbox can't make keyed calls): **5/5 live checks passed.** Connectivity (real Haiku 200 → `IMARA_LIVE_OK`); Ask Imara (live grounded answer correct; off-topic blocked pre-call; scope guard passes real questions); and the decisive one — **native structured outputs WORK live**: a single `output_config.format` call returned 2 schema-valid findings (`severity='high'`), confirming the 2nd parse call can be dropped. **Tier 1.3 is now GO** — gate passed with live evidence. Models confirmed: Sonnet 4.6 + Haiku 4.5. Next: build 1.3 — T1 (make `base_agent._parse_findings` structured = guaranteed-valid JSON, safe, ship now) then T2 (eliminate the 2nd call as a reversible opt-in, A/B'd live via `FULL=1`). NOTE: the v1.17 Tier-1 hardening batch (prose verifier, backups, Ask-Imara guards, observability) is still **unpushed** — `push_imara.bat` ready (21 files).
- **2026-06-21 — v1.22 (BUILD: Tier 1.3 T1 — native structured outputs for the parse call).** Built the safe tier of 1.3 in `base_agent._parse_findings`: the centralized 2nd parse call now uses Anthropic native structured outputs (`output_config.format`, json_schema for the findings array) → **guaranteed schema-valid JSON**, so the parse never degrades to the lossy generic-fallback finding. `_call_claude` gained an `output_config` param with a **graceful TypeError degrade** to a plain call on older SDKs; parsing accepts the structured `{findings:[...]}` shape OR a bare array. SDK floor → `anthropic>=0.69.0`. **233 tests pass** (+4 `test_structured_parse.py`, incl. the degrade path); ruff F + vulture clean; live-mechanism already confirmed (v1.21). Staged in `push_imara.bat` (4 files). **T2 DEFERRED (deliberate):** eliminating the 2nd call (the ~40% cost win) changes finding *quality* and must be settled by a live A/B (`FULL=1`) on a real key — the sandbox can't run keyed calls, so building it blind would break the don't-ship-unverified discipline. T2 is the next cycle once Ruan runs the A/B.
- **2026-06-21 — v1.23 (research cycle: the right plan of action, Ruan-directed).** Researched the GTM/validation question (SA SME-credit landscape; first-design-partner tactics; cold-start validation). Findings: (1) market real + underserved — SA SME funding gap **~ZAR 550bn**, only **~7%** of SMEs get formal bank funding, gap filled by fintech/NBFIs using "advanced analytics" (Imara's lane). (2) Proven first-customer motion = **founder-led sales + a low-friction design-partner pilot**; most under-used channel = a **complementary partner's existing customers** (accountants / Finfind-type matchmakers who already hold SME trust). (3) **Outcome-based validation can't be shortcut** — backtesting needs outcomes that only exist after a real outcome period, so the **outcome clock is the binding constraint**; interim play = convergent validity (Altman Z'', already shipped) + expert face-validity review. → Sharpened Tier 0 into a concrete, time-sensitive plan of action (callout added). Confirms the deferrals (Tax flagship, T2, scaling). No code change — strategy only.
- **2026-06-21 — v1.24 (BUILD: design-partner outreach kit, executing the v1.23 plan).** Built `IMARA_GTM_OUTREACH_KIT.md` to start the outcome clock: a one-pager pitch (ZAR-550bn-gap framing + the honest "Score not yet outcome-calibrated, the pilot earns that" + 8–12wk pilot mechanics), copy-paste outreach emails (lender/NBFI + accountant versions + a follow-up), a target-partner shortlist framework + starter tracker (ideal-partner scoring; sourcing routes: SA NBFIs to verify, SAICA/SAIPA + Xero/QuickBooks advisor directories, Finfind ecosystem; warm intros first), a **face-validity review packet** (2–3 credit pros rate sample reports = interim convergent-validity evidence while outcomes accrue), and a 2-week "start the clock" checklist. The one executable lever I can pull on Tier 0; the selling itself is Ruan-led.
- **2026-06-21 — v1.25 (BUILD: SA Tax-Optimisation agent — the LEGAL "Tax Me If You Can", Ruan-directed "build it, free hand").** Built the on-brand, defensible version (the one the roadmap itself greenlit) — NOT the cross-border migration/loophole flagship (that stays lawyer/validation-gated). New deterministic engine `services/tax_optimizer.py` surfaces legitimate SA reliefs an SME likely QUALIFIES for but may miss: **Small Business Corporation rates (Section 12E)** — computed flat-27%-vs-SBC-graduated saving — plus 12E accelerated allowances, **ETI**, turnover-tax option, SDL position, learnership/R&D flags. Numbers by arithmetic from the dated `sa_rates` corpus (extended with SBC brackets + ETI/SDL); LLM only narrates. **Only confirmed-quantifiable items (SBC) feed the headline saving — the ETI ceiling is never summed (no overstatement).** New `TaxOptimizationAgent` in the independent pipeline tail → `tax_optimization` on SharedMemory + report. **UI:** new `TaxOptimisation.jsx` Dashboard panel + nav entry. **Pressure-tested:** legal line structurally enforced (engine emits ZERO evasion/migration terms across entity types — only named statutory reliefs); **Imara Score NOT corrupted** (the agent is in none of the scored finding-buckets and tax_opt isn't a Score component — pure opportunity overlay, deterministic-first/no-double-count); eligibility edges all correct. 244 tests pass (+14); ruff F + vulture clean; frontend builds (2333 modules). Framed throughout as compliance-positive, GAAR-respecting, "confirm with a registered practitioner" — not tax advice.
- **2026-06-21 — v1.26 (PRESSURE TEST: SA Tax-Optimisation agent, Ruan-directed).** Adversarial cycle on the v1.25 build found + fixed **3 issues**: (1) **non-SA gating bug** — a US/USD business was being shown SA statutory reliefs; now gated to SA context (currency ZAR or country South Africa). (2) **Accounting-negative parsing** — a loss written as `(50,000)` was read as +50,000 and produced a phantom saving; `_fnum` now honours accounting parentheses. (3) **ETI over-display** — a large headcount produced an absurd "up to R3bn" aggregate; ETI is now a clearly per-employee figure (no headcount-multiplied total). **Held clean (no fix needed):** crash-safety on inf/nan/None/non-dict/huge inputs, SBC bracket-boundary maths, ceiling inclusivity (turnover ==R20m / ==R2.3m). +3 regression tests; **249 pass**; ruff F + vulture clean. Fix in `services/tax_optimizer.py`.
- **2026-06-21 — v1.27 (BUILD: POPIA legal/compliance pack, Ruan-directed "analyse roadmap → plan → implement, free hand").** Roadmap analysis: buildable Tier-1 items all shipped; 1.3 T2 gated on a live A/B (Ruan-run); scaling/Tier-3 premature; cross-border Tax flagship lawyer-gated. The highest-value buildable item that **supports distribution rather than adding feature surface** is the legal pack the roadmap flagged as "partly needed now" — a hard prerequisite to running a design-partner pilot on real client financials (POPIA fines to R10M). Researched current POPIA essentials (s18 privacy policy, Information Officer registration, s21 operator agreement, s72 cross-border, breach notification = no materiality threshold, s14 retention) → drafted **4 documents in `legal/`**: Privacy Policy, Terms of Use (decision-support/not-NCA/not-FAIS shield), **Operator Agreement (s21)**, and a cover README with the action list. **Surfaced one genuinely material item:** Imara sends document text to the Anthropic API in the US — a **s72 cross-border transfer** that needs the attorney to confirm the lawful basis (PII redaction + local deterministic compute already mitigate). All clearly DRAFT / not legal advice / needs SA-attorney review. No code change; optional follow-up = wire consent-capture + retention job into the intake.
- **2026-06-21 — v1.28 (BUILD: POPIA technical hooks — make the posture match the v1.27 docs, Ruan-directed "continue").** Wired the technical controls the legal pack assumes: **(1) Retention enforcement (POPIA s14)** — `database.purge_old_analyses` + an opt-in daily daemon (RETENTION_ENABLED, default off; RETENTION_DAYS=365) + admin preview/purge endpoints; pressure-tested (deletes only rows past the cutoff, never recent ones, dry-run never deletes, clamps days>=1, empty-safe). **(2) Consent capture** — a required intake checkbox gating the analysis, carrying `consent`/`consent_at` through client->API, an **auditable `consent_recorded` structured log**, and a plain **cross-border notice** (document text is processed by an AI service outside SA, PII redacted first) + Privacy/Terms links + Information-Officer pointer. Backward-compatible (consent optional at the API; the UI gates it; retention daemon dormant by default). **254 tests pass** (+5 retention; integration test now exercises the consent path); ruff F + vulture clean; frontend builds. (Optional next: backend consent-enforcement flag; publish the Privacy/Terms pages the checkbox links to.)
- **2026-06-21 — v1.29 (BUILD: backend consent enforcement, follow-up).** Added opt-in `REQUIRE_CONSENT` (default off): when on, `/api/analyze` rejects requests without consent with a 400 **before** any DB write or background task — defense-in-depth so a direct API call can't bypass the UI gate. Backward-compatible. +2 isolated rejection-path tests (the accepted path is covered by the integration test); fixed a test-isolation flake by not spawning background pipelines in the new tests. **256 pass**; ruff F + vulture clean. **Deliberately did NOT build the in-app Privacy/Terms pages** — they should render the attorney-FINALISED text, not the current drafts; wiring drafts as live pages would imply finality. Wire once finalised.
- **2026-06-21 — v1.30 (UI AUDIT + fix: exports were missing the Tax agent, Ruan-directed).** Audited every UI surface against this session's changes. **Up to date:** React Dashboard (Tax Me If You Can panel + nav, prose "Narrative check" badge, intake consent + cross-border notice) and the public `SharedReport` (renders the Dashboard, so it inherits all panels). **Found + fixed a real gap:** the **PDF (`report_generator.py`) and self-contained HTML (`html_report.py`) exporters had zero references to `tax_optimization`** — a client/lender receiving an exported report would not see the flagship Tax Me If You Can savings. Added a Tax Me If You Can section to both (headline saving + per-relief eligibility/action + disclaimer), gated on availability with clean omission when absent/non-SA. Consistency note: the exports also omit the faithfulness/prose *verification badges* (screen-only), so no new gap there. +4 export regression tests; **260 pass**; ruff F + vulture clean; frontend builds.
- **2026-06-21 — v1.31 (PRESSURE TEST: found + fixed 2 robustness bugs in the v1.30 export code).** Full pressure test after shipping the Tax-section exports (commit `dd01dbf`): full suite, ratchet, boot, frontend build, full `/api/analyze` e2e exercising the real `/pdf` + `/html` export endpoints (both confirmed carrying the Tax Me If You Can section + SBC relief), then adversarial probes against the exporters. **Two real bugs found in the new HTML exporter and fixed:** (1) `total_saving_high`/`est_saving_high` = `None` (key present, value None) crashed the `:,.0f` format — the `.get(k, 0)` default does not fire on an explicit None; coerced with `or 0`. (2) `opportunities` = `None` crashed the `for` loop in **both** exporters — coerced with `or []` (the PDF shared the same latent gotcha). Confirmed HTML escapes injected `<script>`/`onerror` in tax fields (XSS), and both exporters survive huge/negative/zero/unicode values. +3 regression tests (None-fields, injection). **263 pass**; ruff F clean; verifiers + consent gate still enforce. Lesson logged: the `dict.get(key, default)` default does NOT apply when the key exists with a `None` value — coerce numeric/iterable fields with `or 0` / `or []`.

- **2026-06-21 — v1.32 (DISCIPLINE + housekeeping, agent-led — "focus on what you should do").** With the high-value work either distribution (Ruan-led) or gated on a live A/B, did the one in-lane action the plan has named since v1.3: **codified the coding disciplines into the repo `CLAUDE.md`** — the four rules (think-before-coding, simplicity-first, surgical-changes, goal-driven), the Imara standing disciplines (cleanup ratchet, deterministic-first, research-before-build, pressure-test-every-change, don't-ship-unverified-LLM-changes), and the paid-in-bugs lessons (the `dict.get(key, default)` None gotcha from v1.31; mount-safe editing) — so they bind every future session instead of living only in agent memory. Also **fixed this revision log's ordering**: the v1.17–v1.31 entries had been prepended in reverse above the ascending v1–v1.16 block; the log is now strictly ascending v1 → v1.32. Docs-only; no code change; CI unaffected.

- **2026-06-21 — v1.33 (LIVE FULL verification PASSED on Ruan's machine + OPERATOR_PASSWORD activated).** Two Tier-0/Tier-1 unblocks done together on Ruan's computer. **(a) Login activated:** set `OPERATOR_PASSWORD` on Railway (guided walkthrough); confirmed live — `/api/health` returns `auth_required:true` on commit `9d610561`, so `/api/report/{id}` is no longer a bare-URL bearer read. Closes the OWASP-A01 / POPIA access-control gap (Tier 1.2). **(b) Live A/B gate PASSED:** ran `run_live_verify_full.bat` (FULL=1) against the real API — **9/9 checks passed**. Connectivity (`IMARA_LIVE_OK`), Ask Imara (grounded answer + off-topic blocked pre-call + scope guard), **native structured outputs (single call returns valid structured findings — the Tier 1.3 T2 mechanism, confirmed live)**, and the FULL `/api/analyze` pipeline: completed, report assembled (score+ratios+findings), faithfulness ran over **40 real findings (12 conflicts flagged for review)**, prose verifier ran clean. Pipeline self-reported **~$1.80 over 45 calls**. One transient `529` (overloaded) auto-retried and recovered. **This clears the deferral on structured-outputs T2** — the single-call mechanism is proven live on real findings, so T2 (drop the 2nd parse call, ~40% fewer agent calls) is now GO as its own careful, reversible/opt-in, pressure-tested build cycle. (Helper added: `run_live_verify_full.bat`, local.)

- **2026-06-21 — v1.34 (BUILD: Tier 1.3 T2 — single-call structured findings, opt-in, default OFF; unblocked by v1.33).** New `SINGLE_CALL_FINDINGS` flag (config, default off). `base_agent` gains a central seam `_findings_from(prompt, memory)` and two extracted helpers `_items_from_raw` / `_findings_from_items` shared with the classic parse path (no duplication). **ON:** the specialist's own analysis call emits structured findings in ONE call (`output_config` on the primary model), dropping the 2nd (Haiku) parse call. **OFF (default):** unchanged two-call path. **Any single-call failure or empty result degrades automatically to classic.** Scope correction caught in build: only the **11** sites that return findings directly were routed through it; the **7** that regex-extract numeric blocks (`valuation_mid`/`fraud_risk_level`/`credit_score`/`forecast_*`) from the prose `raw` **stay two-call** (single-call would drop `raw` and NameError — caught by the suite, reverted). Pressure-tested: call-count in both modes, degrade-on-failure, malformed / bare-array / non-dict / empty-findings output. **+7 tests; 270 pass; ruff F + vulture clean; app boots (51 routes).** Activation: after a live on/off quality A/B, set `SINGLE_CALL_FINDINGS=true` on Railway to make it default. (Local helper `run_live_verify_full.bat` added for the A/B.)
